# password_generator
A customizable Python password generator that lets users choose length and character types (uppercase, lowercase, digits, special characters). Easily regenerate passwords or change settings without restarting. Built using simple functions and the random and string modules.
